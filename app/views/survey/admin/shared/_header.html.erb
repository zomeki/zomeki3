<ul class="mainMenu">
<%= render 'layouts/admin/cms/menu', current: :content %>
</ul>

<ul class="contentMenu">
<%= content_tag('li') { link_to "フォーム一覧", survey_forms_path(content: @content), class: current == :form ? 'current' : '' } -%>
<%= content_tag('li') { link_to '設定', survey_content_settings_path, class: current == :setting ? 'current' : '' } if @content.editable? -%>
</ul>
