<ul class="mainMenu">
<%= render 'layouts/admin/cms/menu', current: :content %>
</ul>

<ul class="contentMenu">
<%= content_tag('li') { link_to "#{Survey::Form.model_name.human}一覧", survey_forms_path(content: @content), class: 'fixed' } -%>
<%= content_tag('li') { link_to '設定', survey_content_settings_path, class: 'fixed' } if @content.editable? -%>
</ul>
