<%= @content.upper_reply_text if @content.upper_reply_text.present? %>
<% @form_answer.form.public_questions.where.not(form_type: 'free').each_with_index do |question, index| %>
■<%= question.title %>
<%= @form_answer.answers.where(question_id: question.id).first.try(:content) %>

<% end %>
<%= @content.lower_reply_text if @content.lower_reply_text.present? %>
