<%= render 'menu' %>

<%= form_for @item, as: :item, url: { action: :update, content: @content, doc_id: @doc_id, id: @item.id }, html: { method: :put, multipart: true } do |f| %>
  <%= error_messages_for :item %>
  <%= render 'form', f: f, submit_label: '更新' %>
<% end %>
