<%= action_menu :new if Core.user.has_priv?(:create, item: Core.concept) %>
<%= action_menu :index, main_app.cms_pieces_path %>
<%= action_menu :show %>
<%= action_menu :edit if item && item.editable? && !item.replaced_page? %>
<%= action_menu :destroy if item && item.deletable? %>
<% if params[:action] == 'show' %>
  <% if Core.user.has_priv?(:create, item: Core.concept) %>
  <%= link_to :duplicate, { do: :duplicate }, data: {confirm: '複製してよろしいですか？'} %>
  <% end %>
<% end %>
