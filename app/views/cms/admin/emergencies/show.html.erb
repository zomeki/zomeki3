<table class="show">
  <tr>
    <th><%= @item.class.human_attribute_name :value %></th>
    <td><%= @item.layout.title if @item.layout %></td>
  </tr><tr>
    <th><%= @item.class.human_attribute_name :sort_no %></th>
    <td><%= @item.sort_no %></td>
  </tr>
</table>
