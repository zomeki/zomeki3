<%= render 'menu' %>

<%= form_for @item, as: :item, url: { action: :create }, html: { multipart: true } do |f| %>

<%= error_messages_for :item %>

<%= render 'form', f: f %>

<%= submit :register %>

<% end %>
