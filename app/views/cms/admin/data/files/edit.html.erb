<%= form_for :item, :url => {:action => :update}, :html => {:method => :put, :multipart => true} do |f| %>
  
<%= error_messages_for :item %>

<%= render 'form', f: f %>

<%= submit :edit %>

<% end %>
