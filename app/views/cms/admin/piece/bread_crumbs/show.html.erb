<p class="form">基本情報</p>

<table class="show">
  <%= piece_base_view @item, container: :tr %>
</table>

<p class="form">詳細設定</p>

<table class="show">
  <tr>
    <th><%= @item.class.human_attribute_name :top_label %></th>
    <td><%= @item.setting_value(:top_label) %></td>
  </tr>
  <tr>
    <th><%= @item.class.human_attribute_name :top_link_uri %></th>
    <td><%= @item.setting_value(:top_link_uri) %></td>
  </tr>
</table>

<%= creator_view @item %>
