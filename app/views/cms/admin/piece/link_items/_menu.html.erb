<h2><%= @piece.title %> ： リンク設定</h2>

<div class="actionMenu">
<%= link_to "ピース詳細", cms_piece_link_path(@piece.id) %>
<span class="separator"></span>
<%= action_menu :new     if @piece && @piece.editable? %>
<%= action_menu :index %>
<%= action_menu :show %>
<%= action_menu :edit    if @item && @item.editable? %>
<%= action_menu :destroy if @item && @item.deletable? %>
</div>
