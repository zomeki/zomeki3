<span class="note">※は必須項目です。</span>

<table class="show">
  <tr>
    <th><%= f.label :parent_id %></th>
    <td><%= f.select :parent_id, options_for_select(@item.class.parent_options(Core.user, @item), @item.parent_id), include_blank: true %></td>
  </tr>
  <tr>
    <th><%= f.label :title %> <span class="note">※</span></th>
    <td><%= f.text_field :title, class: 'title' %></td>
  </tr>
  <tr>
    <th><%= f.label :url %> <span class="note">※</span></th>
    <td><%= f.text_field :url, class: 'title' %></td>
  </tr>
  <tr>
    <th><%= f.label :sort_no %></th>
    <td><%= f.text_field :sort_no, class: 'number' %></td>
  </tr>
</table>
