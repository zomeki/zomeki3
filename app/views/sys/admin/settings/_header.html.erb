<ul class="mainMenu">
<%= render 'layouts/admin/cms/menu', current: :setting %>
</ul>

<ul class="contentMenu">
<%-
current = :setting
current = params[:id].to_sym if params[:id].in?(%w(maintenance_mode common_ssl))
-%>
<%= render 'layouts/admin/cms/navi/setting', current: current %>
</ul>
