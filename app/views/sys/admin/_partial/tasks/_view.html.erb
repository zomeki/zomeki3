<%-
publish_task = item.tasks.blank? ? nil : item.tasks.detect(&:publish_task?)
close_task   = item.tasks.blank? ? nil : item.tasks.detect(&:close_task?)
-%>
<p class="form">公開予約設定</p>

<table class="show">
  <tr>
    <th>公開開始日時</th>
    <td><%=l publish_task.process_at if publish_task && publish_task.process_at %></td>
  </tr>
  <tr>
    <th>公開終了日時</th>
    <td><%=l close_task.process_at if close_task && close_task.process_at %></td>
  </tr>
</table>

