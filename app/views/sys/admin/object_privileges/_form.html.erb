<table class="show">
  <tr>
    <th><%= f.label :concept_id %></th>
    <td><%= f.select :concept_id, [["// #{h(Core.site.name)}", '']] + Core.site.concepts_for_option, :include_blank => true, :class => 'id' %></td>
  </tr><tr>
    <th><%= f.label :action %></th>
    <td><%= f.collection_check_boxes :in_actions, @item.class.action_options, :last, :first %></td>
  </tr>
</table>
