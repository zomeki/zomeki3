<%= @from.name %>さんより「<%= @item.title %>」についての承認依頼が届きました。
次の手順により，承認作業を行ってください。

１．PC用記事のプレビューにより文書を確認
<%= Addressable::URI.join(@site.main_admin_uri, @item.preview_uri(params: { node_id: @item.id })) %>

２．次のリンクから承認を実施
<%= Addressable::URI.join(@site.main_admin_uri, @item.admin_uri) %>
