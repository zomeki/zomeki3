<%= render 'search' -%>

<%= page_entries_info @items %>

<table class="index">
  
  <tr>
    <th class="action_link">詳細</th>
    <th style="width: 20%;">ロールID</th>
    <th style="width: ;">ロール名</th>
    <th style="width: 120px; text-align: center;">更新日時</th>
  </tr>
  
  <% @items.each do |item| %>
  <tr class="<%= cycle '', 'cycle' %>">
    <td class="action_link"><%= link_to :show, { action: :show, id: item } %></td>
    <td><%= item.name %></td>
    <td><%= link_to h(item.title), sys_object_privileges_path(item) %></td>
    <td style="text-align: center;"><%=l item.updated_at %></td>
  </tr>
  <% end %>

</table>

<%= paginate @items %>
