<%- if @schedule && @schedule.state == 'performed' -%>
  <div class="notice">組織変更は実行済です。新しい組織変更の登録を始めるには初期化をクリックしてください。</div>
<%- end -%>

<div class="crudTree">
<div class="tree">
  <%- if @root -%>
  <p>
    <%= link_to :show, { action: :show, parent: @root.parent_id.to_i, id: @root } %>
    <%= @root.name %>
  </p>
  <%- end -%>
  <div class="links">
    <ul class="root">
      <%= render 'groups', groups: @groups %>
    </ul>
  </div>
</div>

<div class="content">
<%= render 'users', users: @users if @parent %>
</div>

<div class="footer"></div>
</div>

<script type="text/javascript">
$(function() {
  $('.tree').simpleTree();
});
</script>
